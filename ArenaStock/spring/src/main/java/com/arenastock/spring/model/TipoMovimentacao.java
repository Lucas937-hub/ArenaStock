package com.arenastock.spring.model;

// Enum: so pode assumir um dos dois valores abaixo, nada alem disso
public enum TipoMovimentacao {
    // Movimentacao de entrada de estoque (soma quantidade)
    ENTRADA,
    // Movimentacao de saida de estoque (subtrai quantidade)
    SAIDA
}
